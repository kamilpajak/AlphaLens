"""Market-state signal package (infra side, ADR 0011).

Holds the index-level market-regime "market_state" signal. PR-0 ships the pure
primitives (:mod:`alphalens_pipeline.market.primitives`); PR-1 will add the
``market_state`` classifier + ``enrich`` broadcast stamp on top of them.
"""
