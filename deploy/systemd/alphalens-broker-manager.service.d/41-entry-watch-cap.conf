# Entry-trail watch capacity: 4 concurrent watched picks (the env rail's hard
# MAX; default is 1 per the 2026-08-19 ETSY live incident). Raised on SIM
# 2026-08-27 for the breakeven_trail soak (#1162/#1166 evidence needs several
# concurrent positions, and with ENTRY_TRAIL_BPS=50 every pick routes through
# the watch, so cap=1 serializes the whole queue). SIM-only judgement — the
# LIVE unit keeps its own pin.
[Service]
Environment=ALPHALENS_BROKER_ENTRY_WATCH_MAX_PICKS=4
