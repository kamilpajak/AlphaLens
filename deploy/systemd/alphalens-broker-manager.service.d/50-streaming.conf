# Shared Saxo price stream (on since 2026-07-24). Feeds the entry-trail
# watcher and the live-exit engine; unset means off and the daemon falls back
# to polling. The half-open breaker re-arm (#1102) manages stream loss.
[Service]
Environment=ALPHALENS_BROKER_STREAMING_ENABLED=1
