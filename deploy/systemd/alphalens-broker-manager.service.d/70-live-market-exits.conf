# INC-5 (on since 2026-08-06): the live-exit engine fires laddered
# take-profits from the price feed instead of resting TP orders at the broker
# (locked model docs/research/live_market_execution_*_2026_08_05.md: the
# broker holds ONLY the disaster stop). Turning this on is what made the
# resting-OCO path obsolete — see 90-oco-retired.conf.
[Service]
Environment=ALPHALENS_LIVE_MARKET_EXITS=1
