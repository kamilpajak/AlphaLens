# Master placement arm — REAL MONEY.
#
# The base unit ships 0 (design memo §7 step 1, "deploy inert first"): the
# daemon constructs the LIVE client, keeps the OAuth chain alive, reconciles
# and journals, and places nothing. This file is the §7 step 3 arming, done
# with the operator present after the fire drills passed.
#
# Armed 2026-08-11, between 15:41 and 15:56 CEST. Evidence, from the LIVE
# journal rather than from memory: the last "master arm not set" refusal is
# stamped 15:41:55, and the first refusal that could only come from an ARMED
# daemon (the fee floor rejecting NVAX) is stamped 15:56:12.
#
# Kept OUT of the base unit on purpose. The base unit is a template in a public
# repository; installing it must never be the act that starts trading real
# money. Arming has to be a separate, deliberate file.
[Service]
Environment=ALPHALENS_BROKER_ALLOW_ORDERS=1
